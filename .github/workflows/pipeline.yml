name: pipeline

# Library CI: cut a semver tag + GitHub Release on every code change to main
# (changes limited to .github/ are ignored so CI edits never trigger a
# release), then run the quality gate and build a container image.

on:
  push:
    branches: [main]
    paths-ignore:
      - .github/**
  workflow_dispatch:

jobs:
  guard-semver:
    name: guard-semver
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - name: Block auto-major (Go module path has no /v2 suffix)
        run: |
          LAST_TAG=$(git tag --sort=-version:refname | grep -E ^v[0-9]+\.[0-9]+\.[0-9]+$ | head -1 || echo "")
          if [ -z "$LAST_TAG" ]; then
            echo "No tags yet, skipping"
            exit 0
          fi
          if git log --format="%s" "$LAST_TAG..HEAD" 2>/dev/null | grep -qE "BREAKING CHANGE|!:"; then
            echo "::error::Major bump detected but module path has no /v2 suffix (Go requires /v2 for major>=2). Remove BREAKING CHANGE/!: markers or migrate the module to /v2 manually."
            exit 1
          fi
          echo "No breaking markers: auto-bump stays below v2. OK"

    uses: guilhermelinosp/templates/.github/workflows/release.yml@745ef1ca8288cd320b0b4dcf1485e34003945c79
    with:
      app-client-id: ${{ vars.HELLNET_ACTIONS_CLIENT_ID }}
    secrets: inherit
  quality:
    name: quality
    needs: [release]
    permissions:
      contents: read
      pull-requests: write
    uses: guilhermelinosp/templates/.github/workflows/lib-quality.yml@745ef1ca8288cd320b0b4dcf1485e34003945c79
    with:
    uses: guilhermelinosp/templates/.github/workflows/lib-quality.yml@745ef1ca8288cd320b0b4dcf1485e34003945c79
    with:
      go-version-file: go.mod

  image:
    name: image
    needs: [release, quality]
    permissions:
      contents: read
      packages: write
    uses: guilhermelinosp/templates/.github/workflows/buildah.yml@745ef1ca8288cd320b0b4dcf1485e34003945c79